import Loader from "react-loader-spinner";

const ReactLoader = () => (
  <div className="Loader">
    <Loader type="Grid" color="#f05d00" height={50} width={50} />
  </div>
);

export default ReactLoader;
